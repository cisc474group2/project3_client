https://git-secret.io/#using-gpg

Everyone you need to generate a gpg key and give me your public key and email.id

to reveal the secret, git secret reveal
NEVER allow Config.ts to be added to the repo

gpg --decrypt src/assets/Config.ts.secret src/assets/Config.ts